const Payment = require('../../models/Payment')

// create payment
const makePayment = async (req,res) => {
    try {
        
    } catch (error) {
        res.status(500).json(error)
    }
}

module.exports = {
    makePayment
}